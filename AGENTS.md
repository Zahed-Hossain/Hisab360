# Project Rules & Persistent Instructions

## Navigation & UI Rules
1. **Bottom Navigation Bar (`.bottom-nav`):**
   - Must strictly remain fixed to the 5 core tabs:
     - 🏠 হোম (`dashboard`)
     - 🧮 সাধারণ (`basic`)
     - 📷 QR স্ক্যানার (`qrcode`)
     - 💱 কারেন্সি (`currency`)
     - 📜 ইতিহাস (`history`)
   - **CRITICAL**: Do NOT add any new tools or sections to the bottom navigation bar. Fitness/Health has been deliberately removed from the bottom bar per user directive.

2. **Dashboard Cards Sequence (`.dashboard-grid`):**
   - The cards on the home dashboard must strictly follow this order:
     1. সাধারণ ক্যালকুলেটর (`basic`)
     2. QR কোড স্ক্যানার ও মেকার (`qrcode`)
     3. কারেন্সি কনভার্টার (`currency`)
     4. স্বাস্থ্য ও ফিটনেস ক্যালকুলেটর (`health`)
     5. সায়েন্টিফিক ক্যালকুলেটর (`scientific`)
     6. শতকরা ক্যালকুলেটর (`percentage`)
     7. ফিন্যান্স ক্যালকুলেটর (`finance`)
     8. বয়স ও তারিখ (`age`)
     9. জ্যামিতি ক্যালকুলেটর (`geometry`)
     10. বীজগণিত টুলস (`algebra`)
     11. ভগ্নাংশ ক্যালকুলেটর (`fraction`)
     12. একক রূপান্তর (`converter`)
     13. বাংলাদেশ টুলস (`bangladesh`)
     14. কাস্টম সূত্র (`custom`)
     15. হিসাবের ইতিহাস (`history`)
     16. সেটিংস (`settings`)
